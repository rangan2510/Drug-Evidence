# API Keys - Copy this file to .env and fill in your actual API keys
# REQUIRED API KEYS
OPENAI_API_KEY=                # OpenAI API key (used by GPT-4.1 / GPT-5.2)
ANTHROPIC_API_KEY=             # Anthropic API key (Sonnet / Opus)
GROQ_API_KEY=                  # Groq provider key (optional; OSS models)
TAVILY_API_KEY=                # Tavily web-search (optional; enables baseline arms)

# External service configuration (defaults shown in Settings)
ENTREZ_EMAIL=                 # NCBI Entrez email (required for PubMed E-utilities)
QDRANT_URL=http://localhost:6333   # Qdrant HTTP endpoint (default)
MONGO_URI=mongodb://localhost:27017  # MongoDB for staged evidence (optional)
MONGO_DB_NAME=drug_discovery          # MongoDB database name

# Model ID overrides (optional) — values are provider model strings.
# These mirror the Pydantic `Settings` fields so you can override defaults.
MODEL_GPT_4_1=gpt-4.1-2025-04-14
MODEL_GPT_5_2=gpt-5.2-2025-12-11
MODEL_SONNET_4_5=claude-sonnet-4-5-20250929
MODEL_CLAUDE_OPUS_4_6=claude-opus-4-6

# Legacy / OSS model strings (kept for backward compatibility)
MODEL_GPT_OSS=openai/gpt-oss-120b
MODEL_LLAMA_4=meta-llama/llama-4-maverick-17b-128e-instruct
MODEL_QWEN3=qwen/qwen3-32b
MODEL_KIMI_K2=moonshotai/kimi-k2-instruct-0905

# Notes:
# - Copy this file to `.env` and fill in API keys before running the project.
# - Model override env vars are optional; defaults live in `src/config/settings.py`.
# - `TAVILY_API_KEY` is optional but required if you want baseline web-search arms.

